# DotaLens 项目开发规则

## 核心原则：简洁开发

DotaLens 是本地自用工具。所有设计和实现以“满足当前明确需求、容易运行、容易维护”为准，不按公开商业产品的标准扩张。

## 必须遵守

1. 先实现最短可用路径：同步数据 → 搜索选手 → 查看个人信息和参赛履历。
2. 优先使用现有框架能力和少量直接代码；没有明确收益时不增加依赖、抽象层或基础设施。
3. 默认保持单体应用和本地运行，使用 Next.js、Prisma、SQLite；除非用户明确要求，否则不更换或扩充技术栈。
4. 只实现当前需求，不提前开发可能有用的功能。
5. 数据结构以当前页面和同步需要为准；可以演进，但不为假设中的大规模场景过度设计。
6. Liquipedia 数据只能通过允许的 API 获取，遵守 User-Agent、缓存和限流要求，不抓取普通 HTML 页面。
7. 外部数据至少保留来源链接和最近同步时间，便于人工核对。
8. 修改功能时同步更新相关 README、路线图或数据说明，文档只记录仍然有效的决定。
9. 验证聚焦实际风险：关键解析逻辑写测试，普通界面完成基本运行检查；不追求无意义的覆盖率数字。
10. 遇到数据缺失或格式例外时，优先清晰提示并允许稍后处理，不为少量边缘情况建设复杂系统。

## 默认不做

除非用户明确提出并确认必要性，否则不要增加：

- 用户注册、登录、角色和权限；
- 公共 API、SEO、埋点和运营系统；
- 微服务、Redis、消息队列、对象存储和搜索集群；
- Kubernetes、复杂 CI/CD、高可用和多环境配置；
- 通用插件系统、事件总线和多层领域架构；
- 实时数据、通知推送、预测和比赛级统计；
- 仅为未来可能需求准备的接口、配置项或抽象。

## 技术决策顺序

出现多个实现方案时，依次选择：

1. 代码更少且行为清晰；
2. 依赖更少；
3. 本地启动和备份更简单；
4. 更容易人工检查和修正数据；
5. 在当前数据量下性能足够。

不要以理论上的扩展性取代当前可用性。SQLite 或单进程只有在实际出现容量、并发或稳定性问题后才考虑替换。

## 完成标准

功能满足明确使用场景、关键路径可运行、错误能被看见、相关文档已更新，即可停止。不要在没有新需求的情况下继续重构、优化或扩展。
